.claude/settings.local.json

# Build output
/bin/

# Coverage reports (mise run coverage; uploaded as a CI artifact)
/coverage/

# Local environment (copy of .env.example; may hold local credentials)
.env

# Local wrapped DEK (dev bootstrap; unwrapped by AGE_SECRET_KEY in .env)
local-dek.age

# Python parsing-sidecar local venv + caches (mise run test-parser)
services/parser/.venv/
__pycache__/
*.pyc
.pytest_cache/

