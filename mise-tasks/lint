#!/usr/bin/env bash
#MISE description="Lint Go code (golangci-lint if available, else go vet)"
set -euo pipefail
if command -v golangci-lint >/dev/null 2>&1; then
  golangci-lint run
else
  echo "golangci-lint not found; falling back to 'go vet'" >&2
  go vet ./...
fi
