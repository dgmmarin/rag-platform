# Generated by `mise run openapi` (ragctl openapi) from internal/api/openapi.go.
# Do not edit by hand — the source of truth is the Go code (SPEC-07 §3, ADR-0028).
openapi: 3.1.0
info:
  title: RAG platform public API
  version: v1
  description: Multi-tenant company-knowledge RAG platform. Tenant is derived from the authenticated principal (FR-ACC-03); there is no tenant_id parameter on tenant-scoped routes (SPEC-07 §1).
servers:
  - url: /
    description: Same-origin; base path /v1 (SPEC-07 §1).
tags:
  - name: operational
    description: Health, readiness, metrics, and this spec.
  - name: auth
    description: Session and OIDC authentication.
  - name: platform
    description: Platform-admin surface under /admin (requires is_platform_admin).
  - name: usage
    description: Tenant-scoped usage accounting.
paths:
  /admin/audit:
    get:
      summary: Read a tenant's audit log.
      operationId: adminAuditList
      tags:
        - platform
      security:
        - sessionCookie: []
      parameters:
        - name: tenant
          in: query
          required: true
          description: Tenant id to read the audit log for.
          schema:
            type: string
      responses:
        "200":
          description: audit entries
        "401":
          description: no session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        "403":
          description: not a platform admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /admin/impersonations:
    post:
      summary: Start a platform-admin impersonation grant.
      operationId: adminImpersonationStart
      tags:
        - platform
      security:
        - sessionCookie: []
      responses:
        "201":
          description: grant created
        "401":
          description: no session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        "403":
          description: not a platform admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /admin/impersonations/{id}:
    delete:
      summary: End an impersonation grant.
      operationId: adminImpersonationEnd
      tags:
        - platform
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Impersonation grant id.
          schema:
            type: string
      responses:
        "204":
          description: grant ended
        "401":
          description: no session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        "403":
          description: not a platform admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /healthz:
    get:
      summary: Liveness probe.
      operationId: healthz
      tags:
        - operational
      responses:
        "200":
          description: service is alive
  /metrics:
    get:
      summary: Prometheus metrics.
      operationId: metrics
      tags:
        - operational
      responses:
        "200":
          description: metrics exposition
  /readyz:
    get:
      summary: Readiness probe (control-plane ping).
      operationId: readyz
      tags:
        - operational
      responses:
        "200":
          description: service is ready
        "503":
          description: a readiness check failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /v1/auth/login:
    post:
      summary: Start a session (email + password).
      operationId: authLogin
      tags:
        - auth
      responses:
        "200":
          description: session established
  /v1/auth/logout:
    post:
      summary: Revoke the current session.
      operationId: authLogout
      tags:
        - auth
      responses:
        "200":
          description: session revoked
  /v1/auth/oidc/callback:
    get:
      summary: OIDC redirect callback.
      operationId: oidcCallback
      tags:
        - auth
      responses:
        "200":
          description: session established
  /v1/auth/oidc/start:
    get:
      summary: Begin the OIDC authorization-code flow.
      operationId: oidcStart
      tags:
        - auth
      responses:
        "200":
          description: redirect to the identity provider
  /v1/auth/signup:
    post:
      summary: Create a control-plane user.
      operationId: authSignup
      tags:
        - auth
      responses:
        "200":
          description: user created
  /v1/openapi.json:
    get:
      summary: This OpenAPI document as JSON.
      operationId: openapiJSON
      tags:
        - operational
      responses:
        "200":
          description: the OpenAPI 3.1 document
  /v1/usage:
    get:
      summary: Daily usage rows for the authenticated tenant.
      operationId: usageList
      tags:
        - usage
      security:
        - bearerAuth: []
      parameters:
        - name: from
          in: query
          description: Inclusive start date (YYYY-MM-DD).
          schema:
            type: string
        - name: to
          in: query
          description: Inclusive end date (YYYY-MM-DD).
          schema:
            type: string
      responses:
        "200":
          description: usage rows
        "401":
          description: missing or invalid API key
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        "403":
          description: API key lacks the required scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        "429":
          description: rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
components:
  schemas:
    ErrorEnvelope:
      additionalProperties: false
      properties:
        error:
          additionalProperties: false
          properties:
            code:
              enum:
                - unauthorized
                - forbidden
                - not_found
                - validation
                - rate_limited
                - tenant_unavailable
                - conflict
                - internal
              type: string
            message:
              type: string
            request_id:
              type: string
          required:
            - code
            - message
          type: object
      required:
        - error
      type: object
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 'API key: Authorization: Bearer rk_<prefix>_<secret> (SPEC-07 §2, ADR-0021). Tenant is derived from the key.'
      bearerFormat: rk_<hexprefix>_<secret>
    sessionCookie:
      type: apiKey
      in: cookie
      name: session
      description: Server-side session cookie (SPEC-09 §3). Mutations also require the X-CSRF-Token header.
